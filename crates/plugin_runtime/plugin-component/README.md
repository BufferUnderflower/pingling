# Pingling Component Runtime

Wasmtime Component runtime defaults for WIT-described Pingling package
descriptors.

The default Wasmtime target is `pulley64` so signed macOS daemon builds do not
depend on native JIT code generation.
