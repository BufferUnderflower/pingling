[package]
name = "core-singbox-standalone"
version = "0.1.0"
edition = "2021"

[dependencies]
domain = { path = "../domain" }
util = { path = "../util" }
pingle-config-pipeline = { path = "../pingle-config-pipeline" }
log = "0.4"
serde_json = "1"
